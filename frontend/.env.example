# === TaskFlow Frontend — Variabile de Mediu ===
# Copiază acest fișier ca .env:  cp .env.example .env
# Gata de folosit — nu trebuie să modifici nimic.

# Pentru dezvoltare locală (npm run dev):
VITE_API_URL=http://localhost:5000/api

# Pentru Docker (decomentează linia de mai jos și comentează pe cea de sus):
# VITE_API_URL=/api
