# Frontend API URL — change to your server's IP or domain
# For Docker deployment: leave empty (nginx proxies /api/ to backend)
# For local development: use http://localhost:5000/api
VITE_API_URL=/api
