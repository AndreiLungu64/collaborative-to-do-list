# === TaskFlow — Variabile de Mediu ===
# Acest fișier este gata de folosit. Copiază-l ca .env:
#   cp .env.example .env

# Conexiune PostgreSQL (baza de date partajată a echipei)
DATABASE_URL=postgresql://scraper:Scraper123%23@38.242.226.83:5432/MPI

# Secret JWT pentru semnarea token-urilor de autentificare
JWT_SECRET=taskflow-mpi-2026-super-secret-key

# URL API pentru frontend (în Docker: /api — nginx face proxy)
VITE_API_URL=/api
